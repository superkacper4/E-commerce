import styled from 'styled-components'

const StyledParamsPanel = styled.div`
    width: 100%;
    height: 85vh;
    background-color: ${({ theme }) => theme.colors.primary};

`;

export { StyledParamsPanel }